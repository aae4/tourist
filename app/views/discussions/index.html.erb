<div class="discussions white-form-big">
	<% for discussion in @discussions %>
		<div>
			<h3><%= link_to discussion.name, discussion_path(discussion) %></h3>
			<% if discussion.comments.any? %>
				<small>Last comment by: <%= discussion.comments.last.user.email %> at: <%= discussion.comments.last.created_at %></small>
			<% else %>
				<small>Created by: <%= discussion.user.email %> at: <%= discussion.created_at %></small>
			<% end %>
		</div>
		<% if discussion != @discussions.last %><hr><% end %>
	<% end %>
</div>
<%= link_to t(".new_discussion"), new_discussion_path(:walk_id => params[:walk_id]) %>